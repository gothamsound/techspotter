{
  "name": "techspotter",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-only script breakdown: scene-by-character matrix plus Sound and Video department spotting. All processing happens locally.",
  "type": "module",
  "scripts": {
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "pdfjs-dist": "^5.6.205",
    "xlsx": "^0.18.5"
  }
}
